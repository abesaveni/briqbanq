"""Notifications module — Policies. Notifications are user-self-service."""
# No special policies — users can only access their own notifications.
# This is enforced at the route level via get_current_user.
